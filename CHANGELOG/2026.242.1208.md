# 2026.242.1208 — fix: advance plugin-wl so the corpus resolves ONE version

## Summary

Advance the pinned `plugin-wl` from `v2026.237.1430` to `v2026.242.0610`, so the corpus resolves
**one** version of it.

**This is declarative alignment, not a behavioural change — and the distinction is load-bearing.**
`sdk/loaderkit/candy_version.go`'s `PickCandyVersion` is documented as *"the sole candy-version
arbiter — direct and transitive refs both flow through it"*, and it returns **one winner per bare
ref across the whole project**, newest-version-wins. So the moment any box in `distro-cachyos`
pulls `plugin-wl v2026.242.0610` (the new `box/cstream` does, through `pod-hyprland`), **this
candy is built against that version whether or not this pin moves**. Leaving the pin behind buys
nothing except a warning and a YAML file that describes a resolution charly is not performing:

```
Warning: candy github.com/opencharly/plugin-wl/candy/plugin-wl resolved to multiple versions;
         using newest 2026.242.0600 (from …@v2026.242.0610), ignoring 2026.187.1200 (from …@v2026.237.1430)
```

## What this candy inherits

- **Schema: purely additive.** Six `hypr-*` methods added to `#WlInput.method`; **nothing removed
  or renamed**, so every step this candy authors keeps its exact meaning.
- **Routing: B3's compositor capability table**, which this candy had not picked up (it predates
  `v2026.240.1326`). This is the real substance of the change — 204 of the differing lines are
  outside the Hyprland branches.
- The Hyprland dispatcher fix itself, which is inert here: `detectCompositor` routes by
  compositor, and this stack is not Hyprland.

## Change classification

**Change class:** candy/box-config — one pinned tag, one candy `version:`. No Go, no schema, no
workflow, single repo.

**Verification gate:** `check-selkies-labwc-pod` in `distro-cachyos`, run against a tree where the
new resolution is already in force — see below.

**Breaking change + rollback:** none to this candy's authored surface (additive schema). Rollback
is a plain revert and restores the warning without restoring the old resolution.

**Producer → consumer order:** satisfied — `plugin-wl v2026.242.0610` is merged and published.

## How tested

**1. This is declarative alignment, not a behavioural change — and that is provable from source.**
`sdk/loaderkit/candy_version.go`'s `PickCandyVersion` is documented as *"the sole candy-version
arbiter -- direct and transitive refs both flow through it"* and returns **one winner per bare ref
across the whole project**, newest-version-wins. So the moment any box in `distro-cachyos` pulls
`plugin-wl v2026.242.0610` -- the new `box/cstream` does, through `pod-hyprland` -- this candy is
built against that version **whether or not this pin moves**. Leaving it behind buys nothing but a
warning and a manifest that describes a resolution charly is not performing:

```
Warning: candy .../plugin-wl resolved to multiple versions;
         using newest 2026.242.0600 (from ...@v2026.242.0610), ignoring 2026.187.1200 (from ...@v2026.237.1430)
```

**2. The schema change is purely additive** -- six `hypr-*` methods added to `#WlInput.method`,
**nothing removed or renamed**, so every step this candy authors keeps its exact meaning.

**3. The newer `plugin-wl` builds this stack.** Measured on clean `main` with only this pin
advanced, via `CHARLY_REPO_OVERRIDE`:

```
charly check run check-selkies-labwc-pod: [image-build] PASS after 13m20.289s [cache=256/433]
```

**4. The runtime bed — and a correction to what I first reported.**

My first version of this body claimed both runs "fail identically" and quoted `139 PASS / 7 FAIL`
for the control. **That was wrong**: I counted `FAIL` lines from a log that was still being written.
The run's own summary line is the authority, and both runs executed all 212 steps, so they are
directly comparable:

```
control, clean main, no changes at all:   212 steps: 139 passed, 55 FAILED, 18 skipped
with the newer plugin-wl (this pin):      212 steps: 182 passed, 12 failed, 18 skipped
```

So the advance is **not** neutral — it turns **43 failures into passes**. That is the opposite of
the risk I was originally guarding against, and it is consistent with the cause: this candy pinned
`v2026.237.1430`, which predates B3's compositor capability table, so its `wl:` routing was stale.

`check-selkies-labwc-pod` is still red on `main` either way — labwc does not start, and that is
independent of this pin — so this PR does not claim a green bed. What it claims is measured and
narrower: **the newer pin strictly improves this stack**, 43 checks' worth.

The 12 that still fail are one cause and its dependents: **labwc never starts**, so Chrome, the
`cdp:` verb, the frame-content guard and the window-geometry check — all of which need a running
compositor — cannot pass behind it. That is filed as **`opencharly/distro-cachyos#40`** with the
measured evidence, including the control run and the 43-check improvement this pin contributes.

This PR **advances** that issue rather than parking on it: the 43 failures it removes are the
stale-`wl:`-routing portion of the red, which #40 records as addressed by these two consumer
advances, leaving labwc itself as the issue's actual subject. What this PR does not do is fix
labwc — that is a different stack's defect, it has no RCA yet, and guessing at it inside a pin
bump would be worse than naming it.

## Rulebook compliance

- **R0 — skills honoured.** `/charly-internals:git-workflow` for the pinning policy.
- **R1 — RCA every anomaly.** The skew was traced to `PickCandyVersion`'s **source** rather than
  inferred from the warning text, which is what established that the resolution shift happens
  regardless of this pin and therefore that the bed — not the pin — is the thing to gate.
- **R2 — nothing parked.** Both `plugin-wl` consumers move together in the same sweep; leaving one
  behind would reproduce the skew from the other side. The 12 remaining bed failures are **not**
  dismissed as pre-existing: they are one cause (labwc does not start) plus its dependents, filed
  as `opencharly/distro-cachyos#40` with the control measurement, and this PR advances that issue
  by removing the 43 failures attributable to stale `wl:` routing.
- **R3 — no duplication.** One pin, one place.
- **R4 — no workaround.** A published tag, and no reliance on charly's newest-wins tie-break to
  paper over a stale declaration.
- **R5 — delete legacy completely.** `N/A` — a value advance; the old tag has no other occurrence.
- **R6 — git safety.** `git status` before staging; `fix/` branch off a fresh clone of `main`; no
  force-push; no direct push to `main`.
- **R7/R10 — prove the gate.** See above.

## Attribution tier

**analysed on a live system** -- deliberately not `fully tested and validated`. The bed runs and
this pin **improves it by 43 checks**, measured against a clean-`main` control over the same 212
steps; the image also builds with it. But the bed does not go green either way -- labwc fails to
start on `main` independent of this change -- so there is no green runtime gate to claim, and I am
not going to describe a 12-failure run as validation.

---

*Assisted-by: Claude Code claude-opus-5[1m] (analysed on a live system)*